version: "3.9"

services:
  parrotly-personal-knowledge-assistant:
    build: .
    container_name: parrotly-personal-knowledge-assistant

    ports:
      - "8501:8501"

    env_file:
      - .env

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

    restart: unless-stopped